现有生成物含有：

数据结构类映射
  含 SELECT 方法
  SELECT (限定字段列表)
  UPDATE 方法
  DELETE 方法

 含 存储过程方法（返回DbSet）






todo:

为表现外键关系。添加　子表的 Select(父对象, Query) 方法



todo:

Merge
Distinct
Count
Exists
Sum
Avg
...










todo:
表达式创建器

todo:
为 表达式　的 DateTime 等类型追加　单比较　年月日　等部分　的方法

todo:
为 数据结构->类 的属性 增加 #DEBUG 检查属性在读取的时候是否已填充的功能





todo:

生成对象限定过滤（这会导致外键关系断裂，或SP等返回类型定位不能）